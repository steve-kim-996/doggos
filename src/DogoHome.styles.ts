import { makeStyles } from 'tss-react/mui'

export const useStyles = makeStyles()(() => ({
    doggoContainer: {
        display: 'flex',
        justifyContent: 'center',
        padding: '20px',
    }
}))